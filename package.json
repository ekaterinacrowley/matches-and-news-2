{
  "name": "html-scss-js-codespace-template",
  "version": "1.0.0",
  "scripts": {
    "watch:scss": "sass --watch src/scss/main.scss:dist/styles.css --style=expanded",
    "watch:js": "esbuild --bundle src/js/main.js --outfile=dist/main.js --watch",
    "watch:html": "node watch-html.js",
    "watch:html-build": "node watch-posthtml.js",
    "watch:images": "node -e 'require(\"chokidar\").watch(\"src/images\", {ignoreInitial:true}).on(\"all\", (event, path) => { console.log(new Date().toISOString(), event, path); require(\"child_process\").execSync(\"npm run copy:images\", {stdio: \"inherit\"}); })'",
    "watch:js-files": "node -e 'require(\"chokidar\").watch(\"src/js\", {ignoreInitial:true}).on(\"all\", (event, path) => { console.log(new Date().toISOString(), event, path); require(\"child_process\").execSync(\"npm run copy:js\", {stdio: \"inherit\"}); })'",
    "serve": "live-server dist --port=5000 --watch=dist",
    "copy:images": "mkdir -p dist/images && cp -r src/images/* dist/images/",
    "copy:components": "cp -r src/components dist/components",
    "copy:js": "mkdir -p dist/js && cp -r src/js/* dist/js/",
    "build": "node scripts/inject-env-html.js && esbuild src/js/main.js --bundle --outfile=dist/main.js && npx posthtml src/index.html -o dist/index.html -c posthtml.config.js && cp -r src/scss dist/scss && npm run copy:images && npm run copy:components",
    "dev": "npm-run-all --parallel watch:scss watch:js watch:js-files watch:images watch:html-build start copy:images copy:components copy:js",
    "start": "node server.js",
    "proxy": "node watch-html.js"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "dotenv": "^16.0.0",
    "esbuild": "^0.18.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "posthtml": "^0.16.6",
    "posthtml-cli": "^0.10.0",
    "posthtml-include": "^1.7.4",
    "sass": "^1.75.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "swiper": "^11.2.10"
  },
  "description": "## Установка и запуск проекта",
  "main": "posthtml.config.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
